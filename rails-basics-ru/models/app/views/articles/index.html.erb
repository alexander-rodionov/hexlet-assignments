<h1>Articles</h1>
<table>
    <tr><th>Title</th><th>Date</th></tr>
    <% @articles.each do |a| %>
    <tr><td><%= link_to a.title, article_path(a.id) %></td><td><%= a.created_at.to_date %></td></tr>
    <% end %>
</table>